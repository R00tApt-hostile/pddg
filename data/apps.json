// 1. Create data/apps.json
{
  "tools": [
    // All existing tools plus new ones
    {
      "id": 21,
      "name": "Tor Browser",
      "url": "https://www.torproject.org",
      "description": "Anonymous web browser that routes traffic through the Tor network.",
      "category": "browser",
      "openSource": true,
      "decentralized": true,
      "privacyLevel": "high",
      "platforms": ["Windows", "macOS", "Linux", "Android"],
      "difficulty": "intermediate"
    }
  ]
}

// 2. Update script.js to load from JSON
async function loadToolsData() {
  try {
    const response = await fetch('data/apps.json');
    const data = await response.json();
    window.tools = data.tools;
    renderTools();
    updateStats();
  } catch (error) {
    console.error('Error loading tools data:', error);
    // Fallback to embedded data
    window.tools = defaultTools;
  }
}
